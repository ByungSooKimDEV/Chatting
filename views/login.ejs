<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" />
		<meta http-equiv="X-UA-Compatible" content="ie=edge" />
		<meta name="apple-mobile-web-app-capable" content="yes" />
		<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
		<link
			rel="stylesheet"
			href="https://use.fontawesome.com/releases/v5.8.1/css/all.css"
			integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf"
			crossorigin="anonymous"
		/>
		<link rel="apple-touch-icon" href="images/kakaofriends.png" />
		<link rel="apple-touch-startup-image" href="images/kakaofriends.png" />
		<title>Login</title>
		<link rel="stylesheet" href="css/style.css" />
	</head>
	<body>
		<header class="top-header top-header--transparent">
			<div class="header__top">
				<div class="header__column">
					<i class="fas fa-plane"></i>
					<i class="fas fa-wifi"></i>
				</div>
				<div class="header__column">
					<span class="header__time">18:38</span>
				</div>
				<div class="header__column">
					<i class="fas fa-moon"></i>
					<i class="fab fa-bluetooth-b"></i>
					<span class="header__battery">66% <i class="fas fa-battery-three-quarters"></i></span>
				</div>
			</div>
		</header>
		<main class="login">
			<div>
				<i class="fas fa-comment fa-10x"></i>
			</div>
			<!-- <form action="/login" method="POST" id="loginForm">
				<div class="type-login__input">
					<input type="text" name="userId" id="userId" />
				</div>
				<div class="type-login__input">
					<input type="password" name="password" id="password" />
				</div>
				<div class="type-login__input" style="text-align: center">
					<button type="submit" class="loginBtn">로그인dlek</button>
				</div>
			</form> -->
			<form action="/login" method="POST" id="loginForm">
				<div class="type-login__input">
					<input type="text" name="userId" id="userId" />
				</div>
				<div class="type-login__input">
					<input type="password" name="password" id="password" />
				</div>
				<div class="type-login__input" style="text-align: center">
					<button type="submit" class="loginBtn">로그인</button>
				</div>
			</form>

			<!-- <div id="loginForm">
				<div class="type-login__input">
					<input type="text" name="userId" id="userId"/>
				</div>
				<div class="type-login__input">
					<input type="password" name="password" id="password"/>
				</div>
				<div class="type-login__input" style="text-align: center;">
					<input type="button" class="loginBtn" value="로그인" onClick="onLoginSubmit()"></input>
				</div>
			</div> -->
		</main>
		<div class="bigScreenText">
			<span>Please make your screen smaller</span>
		</div>

		<script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>

		<script>
			// const userIdInput = document.querySelector('#userId');
			// const passwordInput = document.querySelector('#password');

			// function onLoginSubmit(event) {
			// 	const userId = userIdInput.value;
			// 	const password = passwordInput.value;

			// 	axios
			// 		.post('http://localhost:7000/login', { userId, password }, { headers: { 'Content-Type': 'application/json' } })
			// 		.then(function (response) {
			// 			console.log(`로그인 결과 확인 ${JSON.stringify(response)}`);
			// 			console.log(`로그인 결과 확인 ${JSON.stringify(response.data)}`);
			// 			console.log(`로그인 결과 확인 ${JSON.stringify(response)}`);
			// 			if (response.data && response.data.message && response.data.message === '로그인 성공') {
			// 				localStorage.setItem('userInfo', JSON.stringify(response.data[0]));
			// 				location.href = '/index';
			// 			} else {
			// 				alert('로그인 실패');
			// 			}
			// 		})
			// 		.catch(function (error) {
			// 			console.error(error);
			// 		});

			// axios.post('http://localhost:7000/login', {
			// 	userId, password
			// 	}, {
			// 	headers: {
			// 		'Content-Type': 'application/json'
			// 	}
			// 	})
			// 	.then(function(response) {
			// console.log(`로그인 결과 확인 ${JSON.stringify(response)}`);
			// console.log(`로그인 결과 확인 ${JSON.stringify(response.data)}`);
			// console.log(`로그인 결과 확인 ${JSON.stringify(response)}`);
			// if(response.data && response.data.message && response.data.message == "로그인 성공") {
			// 	// localStorage.setItem("userInfo", JSON.stringify(response.data[0]))
			// 	location.href='index.html'
			// } else {
			// 	alert("로그인 실패")
			// }
			// })
			// .catch(function(error) {
			// 	console.error(error);
			// });
			// }

			// function paintGreeting(username){
			// 	greeting.innerText = `Hello ${username}`;
			// 	greeting.classList.remove(HIDDEN_CLASSNAME);
			// }

			// const savedUsername = localStorage.getItem(USERNAME_KEY);
			// if(savedUsername === null){
			// 	loginForm.classList.remove(HIDDEN_CLASSNAME);
			// 	loginForm.addEventListener("submit", onLoginSubmit);
			// } else {
			// 	paintGreeting(savedUsername);
			// }
		</script>
	</body>
</html>
